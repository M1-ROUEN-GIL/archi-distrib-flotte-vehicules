{
  "name": "flotte-frontend-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "build:remotes": "npm run build --prefix apps/location && npm run build --prefix apps/vehicles && npm run build --prefix apps/drivers && npm run build --prefix apps/maintenance",
    "start:all": "concurrently \"npm run preview --prefix apps/location\" \"npm run preview --prefix apps/vehicles\" \"npm run preview --prefix apps/drivers\" \"npm run preview --prefix apps/maintenance\" \"npm run dev --prefix apps/shell\"",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "lucide-react": "^1.8.0"
  },
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
