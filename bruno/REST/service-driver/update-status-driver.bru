meta {
  name: update-status-driver
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/drivers/{{rest_driver_id}}/status
  body: json
  auth: inherit
}

body:json {
  {
    "status": "ON_TOUR"
  }
}
