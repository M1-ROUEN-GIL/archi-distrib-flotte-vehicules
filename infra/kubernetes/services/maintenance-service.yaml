apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: flotte-namespace
spec:
  selector:
    app: maintenance-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP